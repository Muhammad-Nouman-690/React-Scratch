const INITIAL_STATE = {
    users: [
        {
            name: 'Muhammad Noman',
            email: 'nomi690@hotmail.com'
        }
    ]
}

export default (state = INITIAL_STATE, action ) => {
    return state;
}